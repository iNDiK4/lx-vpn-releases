<Window x:Class="XrayLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LX VPN"
        Icon="icon.ico"
        Height="550" Width="850"
        AllowsTransparency="True" WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        MouseDown="Window_MouseDown">
    
    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="BgBrush" Color="#0d0d1a"/>
        <SolidColorBrush x:Key="CardBrush" Color="#1a1a2e"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#6366f1"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#22d3ee"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#10b981"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#ef4444"/>
        
        <!-- ScrollBar Style -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#3a3a5e" CornerRadius="4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Glassmorphism Card -->
        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="#1a1a2e"/>
            <Setter Property="BorderBrush" Value="#2a2a4e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
        
        <!-- Sidebar Button -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a4e"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SidebarButtonActive" TargetType="Button" BasedOn="{StaticResource SidebarButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <!-- TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0d0d1a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2a2a4e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Action Button -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Toggle Switch -->
        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="track" Width="50" Height="26" CornerRadius="13" Background="#2a2a4e"/>
                            <Border x:Name="thumb" Width="22" Height="22" CornerRadius="11" Background="White" 
                                    HorizontalAlignment="Left" Margin="2,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Window Buttons -->
        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a4e"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="Transparent" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#ef4444"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="box" Width="20" Height="20" CornerRadius="4" 
                                    Background="#2a2a4e" BorderBrush="#3a3a5e" BorderThickness="1"/>
                            <TextBlock x:Name="check" Text="✓" Foreground="White" FontSize="12" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Site Card -->
        <Style x:Key="SiteCard" TargetType="Border">
            <Setter Property="Background" Value="#0d0d1a"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <!-- Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0d0d1a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2a2a4e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" Content="{TemplateBinding SelectionBoxItem}" 
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      VerticalAlignment="Center" Margin="10,0,0,0"/>
                                    <TextBlock Grid.Column="1" Text="▼" Foreground="#888" FontSize="10"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <ToggleButton Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Background="Transparent" BorderThickness="0" Opacity="0"/>
                                </Grid>
                            </Border>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" 
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="#1a1a2e" BorderBrush="#2a2a4e" BorderThickness="1" 
                                        CornerRadius="8" Margin="0,5,0,0" Padding="5">
                                    <ScrollViewer MaxHeight="200">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a4e"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- RGB Border Wrapper -->
    <Border x:Name="RgbBorder" CornerRadius="17" BorderThickness="1" BorderBrush="#6366f1">
        <Border CornerRadius="16" Background="{StaticResource BgBrush}" BorderBrush="#1a1a2e" BorderThickness="0">
        <Grid>
            <!-- Ambient Glow -->
            <Ellipse x:Name="AmbientGlow" Width="400" Height="400" Opacity="0.15" IsHitTestVisible="False"
                     HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-100,0,0">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#6366f1" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Sidebar -->
                <Border Grid.Column="0" Background="#0a0a14" CornerRadius="16,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,30">
                            <TextBlock Text="LX" FontSize="24" FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="VPN" FontSize="10" Foreground="#666" HorizontalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center">
                            <Button x:Name="NavHome" Style="{StaticResource SidebarButtonActive}" Content="🏠" FontSize="20" ToolTip="Главная" Click="NavHome_Click"/>
                            <Button x:Name="NavSplit" Style="{StaticResource SidebarButton}" Content="⚡" FontSize="20" ToolTip="Split Tunneling" Click="NavSplit_Click"/>
                            <Button x:Name="NavSettings" Style="{StaticResource SidebarButton}" Content="⚙" FontSize="20" ToolTip="Настройки" Click="NavSettings_Click"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <Button x:Name="TrayButton" Style="{StaticResource SidebarButton}" Content="📥" FontSize="18" ToolTip="В трей" Click="TrayBtn_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Main Content -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Title Bar -->
                    <Grid Grid.Row="0" Margin="20,10,10,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <TextBlock Text="LX VPN" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                            <Border Background="#10b981" CornerRadius="4" Padding="8,3" Margin="15,0,0,0">
                                <TextBlock x:Name="HeaderVersionLabel" Text="v2.4.0" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button Style="{StaticResource WindowButton}" Content="─" Click="MinimizeBtn_Click"/>
                            <Button Style="{StaticResource CloseButton}" Content="✕" Margin="5,0,0,0" Click="CloseBtn_Click"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Pages -->
                    <Grid Grid.Row="1" Margin="20,10,20,20">
                        
                        <!-- HOME PAGE -->
                        <Grid x:Name="HomePage">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <!-- Connect Button with spinner -->
                                <Grid Width="200" Height="200" HorizontalAlignment="Center">
                                    <!-- Shadow/Glow -->
                                    <Ellipse x:Name="ConnectButtonGlow" Width="200" Height="200" Opacity="0.4" 
                                             HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Color="#6366f1" Offset="0.4"/>
                                                <GradientStop Color="Transparent" Offset="1"/>
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    
                                    <!-- Spinning ring (shown when connecting) -->
                                    <Ellipse x:Name="SpinnerRing" Width="170" Height="170" StrokeThickness="3"
                                             HorizontalAlignment="Center" VerticalAlignment="Center"
                                             Visibility="Collapsed" RenderTransformOrigin="0.5,0.5">
                                        <Ellipse.Stroke>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#6366f1" Offset="0"/>
                                                <GradientStop Color="Transparent" Offset="0.7"/>
                                            </LinearGradientBrush>
                                        </Ellipse.Stroke>
                                        <Ellipse.RenderTransform>
                                            <RotateTransform x:Name="SpinnerRotation" Angle="0"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    
                                    <!-- Button -->
                                    <Button x:Name="ConnectButton" Width="150" Height="150" 
                                            HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Click="ToggleButton_Click" Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid>
                                                    <Ellipse x:Name="ButtonBg" Width="150" Height="150">
                                                        <Ellipse.Fill>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                <GradientStop Color="#6366f1" Offset="0"/>
                                                                <GradientStop Color="#4f46e5" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Ellipse.Fill>
                                                    </Ellipse>
                                                    <Ellipse Width="130" Height="130" Opacity="0.15" VerticalAlignment="Top" Margin="0,8,0,0">
                                                        <Ellipse.Fill>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                <GradientStop Color="White" Offset="0"/>
                                                                <GradientStop Color="Transparent" Offset="0.5"/>
                                                            </LinearGradientBrush>
                                                        </Ellipse.Fill>
                                                    </Ellipse>
                                                    <TextBlock x:Name="ButtonIcon" Text="⏻" FontSize="50" Foreground="White" 
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                                
                                <!-- Session Timer -->
                                <TextBlock x:Name="SessionTimer" Text="" FontSize="14" Foreground="{StaticResource AccentBrush}" 
                                           HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
                                
                                <TextBlock x:Name="ConnectionStatus" Text="ОТКЛЮЧЕНО" FontSize="20" FontWeight="Bold" 
                                           Foreground="#888" HorizontalAlignment="Center" Margin="0,10,0,5"/>
                                <TextBlock x:Name="StatusDetails" Text="Нажмите для подключения" FontSize="13" 
                                           Foreground="#555" HorizontalAlignment="Center"/>
                            </StackPanel>
                            
                            <!-- Info Cards -->
                            <Grid Grid.Row="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="10"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Top row - Traffic stats -->
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Upload -->
                                    <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="15">
                                        <StackPanel>
                                            <TextBlock Text="↑ UPLOAD" FontSize="11" Foreground="#888"/>
                                            <TextBlock x:Name="UploadLabel" Text="0 KB/s" FontSize="18" FontWeight="Bold" 
                                                       Foreground="{StaticResource AccentBrush}" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="UploadTotal" Text="Всего: 0 MB" FontSize="11" Foreground="#555" Margin="0,3,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Download -->
                                    <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="15">
                                        <StackPanel>
                                            <TextBlock Text="↓ DOWNLOAD" FontSize="11" Foreground="#888"/>
                                            <TextBlock x:Name="DownloadLabel" Text="0 KB/s" FontSize="18" FontWeight="Bold" 
                                                       Foreground="{StaticResource SuccessBrush}" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="DownloadTotal" Text="Всего: 0 MB" FontSize="11" Foreground="#555" Margin="0,3,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                                
                                <!-- Bottom row - IP, Ping, Mode -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="12">
                                        <StackPanel>
                                            <TextBlock Text="🌐 IP" FontSize="10" Foreground="#888"/>
                                            <TextBlock x:Name="IpLabel" Text="Загрузка..." FontSize="12" FontWeight="SemiBold" 
                                                       Foreground="White" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="12">
                                        <StackPanel>
                                            <TextBlock Text="📡 ПИНГ" FontSize="10" Foreground="#888"/>
                                            <TextBlock x:Name="PingLabel" Text="-- ms" FontSize="12" FontWeight="SemiBold" 
                                                       Foreground="White" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="4" Style="{StaticResource GlassCard}" Padding="12">
                                        <StackPanel>
                                            <TextBlock Text="⚡ РЕЖИМ" FontSize="10" Foreground="#888"/>
                                            <TextBlock x:Name="ModeLabel" Text="Split" FontSize="12" FontWeight="SemiBold" 
                                                       Foreground="{StaticResource AccentBrush}" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Grid>
                        
                        <!-- SPLIT PAGE -->
                        <Grid x:Name="SplitPage" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <Border Grid.Row="0" Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="⚡ Split Tunneling" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="Только выбранные сайты через VPN" FontSize="12" 
                                                   Foreground="#888" Margin="0,5,0,0"/>
                                    </StackPanel>
                                    <CheckBox x:Name="SplitToggle" Style="{StaticResource ToggleSwitch}" IsChecked="True"
                                              HorizontalAlignment="Right" VerticalAlignment="Center"
                                              Checked="SplitToggle_Changed" Unchecked="SplitToggle_Changed"/>
                                </Grid>
                            </Border>
                            
                            <Border Grid.Row="1" Style="{StaticResource GlassCard}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                                        <StackPanel x:Name="SitesList">
                                            <!-- Все заблокированные сайты -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkBlocked" IsChecked="True" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border Background="#ef4444" CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <TextBlock Text="🚫" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="Все заблокированные сайты" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock x:Name="BlockedCountLabel" Text="~200 доменов (RU blacklist)" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            
                                            <!-- Discord -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkDiscord" IsChecked="True" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border Background="#5865F2" CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <TextBlock Text="💬" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="Discord" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock Text="discord.com, discordapp.com" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            
                                            <!-- Telegram -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkTelegram" IsChecked="True" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border Background="#2AABEE" CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <TextBlock Text="✈" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="Telegram" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock Text="telegram.org, t.me" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            
                                            <!-- YouTube -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkYouTube" IsChecked="True" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border Background="#FF0000" CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <TextBlock Text="▶" FontSize="14" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="YouTube" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock Text="youtube.com, googlevideo.com" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            
                                            <!-- Twitter -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkTwitter" IsChecked="False" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border Background="#000" CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <TextBlock Text="𝕏" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="Twitter / X" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock Text="twitter.com, x.com" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            
                                            <!-- Instagram -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkInstagram" IsChecked="False" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <Border.Background>
                                                            <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                                                                <GradientStop Color="#f09433" Offset="0"/>
                                                                <GradientStop Color="#bc1888" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Border.Background>
                                                        <TextBlock Text="📷" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="Instagram" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock Text="instagram.com" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            
                                            <!-- Spotify -->
                                            <Border Style="{StaticResource SiteCard}">
                                                <StackPanel Orientation="Horizontal">
                                                    <CheckBox x:Name="ChkSpotify" IsChecked="False" VerticalAlignment="Center" Cursor="Hand"/>
                                                    <Border Background="#1DB954" CornerRadius="8" Width="36" Height="36" Margin="12,0">
                                                        <TextBlock Text="🎵" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="Spotify" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                        <TextBlock Text="spotify.com" FontSize="11" Foreground="#666"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </ScrollViewer>
                                    
                                    <Grid Grid.Row="1" Margin="0,15,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="CustomDomainBox" Grid.Column="0" Style="{StaticResource ModernTextBox}" 
                                                 Text="" ToolTip="example.com"/>
                                        <Button Grid.Column="1" Content="+ Добавить" Style="{StaticResource ActionButton}" 
                                                Margin="10,0,0,0" Click="AddCustomDomain_Click"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                        
                        <!-- SETTINGS PAGE -->
                        <Grid x:Name="SettingsPage" Visibility="Collapsed">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="⚙ Настройки" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                                    
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="80"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Тип прокси" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                <TextBlock Text="SOCKS5 для Discord, HTTP для браузера" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <ComboBox x:Name="ProxyTypeCombo" Grid.Column="1" Width="100" Height="32" Margin="0,0,15,0"
                                                      Background="#0d0d1a" Foreground="White" BorderBrush="#2a2a4e" SelectedIndex="0">
                                                <ComboBoxItem Content="HTTP" Tag="http"/>
                                                <ComboBoxItem Content="SOCKS5" Tag="socks"/>
                                            </ComboBox>
                                            <TextBox x:Name="PortInput" Grid.Column="2" Style="{StaticResource ModernTextBox}" 
                                                     Text="10808" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="Сервер" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                            <Grid Margin="0,10,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Адрес" FontSize="11" Foreground="#666"/>
                                                    <TextBlock Text="45.90.99.219:16933" FontSize="13" Foreground="{StaticResource AccentBrush}" Margin="0,3,0,0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Протокол" FontSize="11" Foreground="#666"/>
                                                    <TextBlock Text="VLESS + Reality" FontSize="13" Foreground="{StaticResource AccentBrush}" Margin="0,3,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Auto-connect -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <StackPanel>
                                                <TextBlock Text="Автоподключение" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                <TextBlock Text="Подключаться к VPN при запуске приложения" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <CheckBox x:Name="AutoConnectToggle" Style="{StaticResource ToggleSwitch}" IsChecked="False"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Checked="AutoConnectToggle_Changed" Unchecked="AutoConnectToggle_Changed"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Auto-start with Windows -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <StackPanel>
                                                <TextBlock Text="Запуск с Windows" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                <TextBlock Text="Запускать LX VPN при входе в систему" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <CheckBox x:Name="AutoStartToggle" Style="{StaticResource ToggleSwitch}" IsChecked="False"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Checked="AutoStartToggle_Changed" Unchecked="AutoStartToggle_Changed"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Kill Switch -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🛡️ Kill Switch" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                    <Border Background="#6366f1" CornerRadius="3" Padding="5,2" Margin="10,0,0,0">
                                                        <TextBlock Text="NEW" FontSize="9" Foreground="White" FontWeight="Bold"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="Блокировать интернет при обрыве VPN" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <CheckBox x:Name="KillSwitchToggle" Style="{StaticResource ToggleSwitch}" IsChecked="False"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Checked="KillSwitchToggle_Changed" Unchecked="KillSwitchToggle_Changed"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- DNS-over-HTTPS -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <StackPanel>
                                                <TextBlock Text="🔒 DNS-over-HTTPS" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                <TextBlock Text="Безопасный DNS через Cloudflare" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <CheckBox x:Name="DoHToggle" Style="{StaticResource ToggleSwitch}" IsChecked="True"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Checked="DoHToggle_Changed" Unchecked="DoHToggle_Changed"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- RGB Border -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🌈 RGB Подсветка" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                    <Border Background="#6366f1" CornerRadius="3" Padding="5,2" Margin="10,0,0,0">
                                                        <TextBlock Text="NEW" FontSize="9" Foreground="White" FontWeight="Bold"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="Анимированная RGB рамка вокруг окна" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <CheckBox x:Name="RgbToggle" Style="{StaticResource ToggleSwitch}" IsChecked="True"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Checked="RgbToggle_Changed" Unchecked="RgbToggle_Changed"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Speed Test & WebRTC -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="🚀 Тесты" FontSize="14" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <Button Grid.Column="0" Content="⚡ Speed Test" Style="{StaticResource ActionButton}"
                                                        Click="SpeedTest_Click"/>
                                                <Button Grid.Column="2" Content="🔍 WebRTC Check" Click="WebRTCCheck_Click"
                                                        Background="#2a2a4e" Foreground="White" BorderThickness="0" 
                                                        Padding="15,12" FontSize="13" Cursor="Hand"/>
                                            </Grid>
                                            <TextBlock x:Name="SpeedTestResult" Text="" FontSize="12" Foreground="{StaticResource AccentBrush}" 
                                                       Margin="0,10,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Updates -->
                                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,15">
                                        <Grid>
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🔄 Обновления" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                    <TextBlock x:Name="CurrentVersionLabel" Text="v2.0.0" FontSize="11" Foreground="#666" 
                                                               Margin="10,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBlock Text="Проверить наличие новой версии" FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <Button Content="Проверить" Style="{StaticResource ActionButton}" 
                                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                                    Padding="15,8" Click="CheckUpdate_Click"/>
                                        </Grid>
                                    </Border>
                                    
                                    <Border Style="{StaticResource GlassCard}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                <TextBlock Text="Логи" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                <Button Content="Очистить" FontSize="11" Background="#2a2a4e" Foreground="#888" 
                                                        BorderThickness="0" Padding="10,3" Margin="15,0,0,0" Cursor="Hand"
                                                        Click="ClearLogs_Click"/>
                                                <Button Content="Показать конфиг" FontSize="11" Background="#2a2a4e" Foreground="#888" 
                                                        BorderThickness="0" Padding="10,3" Margin="10,0,0,0" Cursor="Hand"
                                                        Click="ShowConfig_Click"/>
                                            </StackPanel>
                                            <Border Background="#0a0a14" CornerRadius="8" Padding="12" Height="200">
                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                    <TextBox x:Name="LogBox" Background="Transparent" BorderThickness="0" 
                                                             Foreground="{StaticResource AccentBrush}" FontFamily="Consolas" FontSize="11" 
                                                             IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"/>
                                                </ScrollViewer>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                        
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
    </Border>
</Window>
