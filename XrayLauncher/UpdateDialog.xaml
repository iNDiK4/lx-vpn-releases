<Window x:Class="XrayLauncher.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление"
        Height="320" Width="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Topmost="True">
    
    <Window.Resources>
        <!-- Custom Scrollbar Style -->
        <Style x:Key="DarkScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#3a3a5e" CornerRadius="4" Margin="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="16" Background="#1a1a2e" BorderBrush="#2a2a4e" BorderThickness="1">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <TextBlock Text="🎉" FontSize="24" VerticalAlignment="Center"/>
                <TextBlock Text=" Доступно обновление!" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Version info -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <TextBlock x:Name="OldVersionText" Text="v2.0.0" FontSize="14" Foreground="#888"/>
                <TextBlock Text=" → " FontSize="14" Foreground="#888"/>
                <TextBlock x:Name="NewVersionText" Text="v2.1.0" FontSize="14" Foreground="#22d3ee" FontWeight="Bold"/>
            </StackPanel>
            
            <!-- Changelog -->
            <Border Grid.Row="2" Background="#0d0d1a" CornerRadius="8" Padding="12" Margin="0,0,0,15">
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <TextBlock x:Name="ChangelogText" Text="• Добавлены новые функции&#x0a;• Исправлены ошибки" 
                               FontSize="13" Foreground="#ccc" TextWrapping="Wrap" LineHeight="22"/>
                </ScrollViewer>
            </Border>
            
            <!-- Progress bar -->
            <Grid Grid.Row="3" Margin="0,0,0,15">
                <ProgressBar x:Name="DownloadProgress" Height="6" Background="#2a2a4e" Foreground="#6366f1" 
                             Value="0" Visibility="Collapsed"/>
                <TextBlock x:Name="DownloadStatus" Text="" FontSize="11" Foreground="#888" 
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </Grid>
            
            <!-- Buttons -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button x:Name="UpdateBtn" Grid.Column="0" Content="⬇ Обновить" Click="Update_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#6366f1" CornerRadius="8" Padding="15,12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Grid.Column="2" Content="Позже" Click="Later_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#2a2a4e" CornerRadius="8" Padding="15,12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#3a3a5e"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
